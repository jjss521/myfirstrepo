# 2026-06-22 工作日志

## PySide6 GUI 重写完成

### 改动的 3 个文件

**1. `gui_app_pyside6.py`**（新建，PySide6 主窗口）
- 等价替换 `gui_app.py`（CustomTkinter 版）
- 使用 `QMainWindow` + `QVBoxLayout`/`QHBoxLayout` 布局
- QSS 样式表实现暗色主题（#1a1a2e 背景，#0d9488 主色）
- 功能完整：文本输入、文件导入、剪贴板粘贴、解析预览、参数配置、开始/停止/打开报告、标签页日志/结果/报告

**2. `proofread_window_pyside6.py`**（新建，PySide6 校对窗口）
- 等价替换 `proofread_window.py`
- 功能完整：文本输入、仅解析、在线校对、进度条、结果彩色显示、详细表格、纠正文本、复制结果

**3. `run_gui.py`**（更新入口）
- 改为 `from gui_app_pyside6 import launch`

### 验证结果
- 所有模块导入正常（config/models/standard_parser/utils/web_scraper/report_generator/gui_app_pyside6/proofread_window_pyside6）
- 主窗口 12 个关键控件全部存在
- 文本解析功能正常（2条标准正确解析）

### 待确认
- 原 `gui_app.py` 和 `proofread_window.py`（CustomTkinter 版）保留，可选择删除或保留作为备份
- PyInstaller 打包如需 EXE，需更新 `install_deps.bat`/`install_deps.py` 及 `.spec` 文件
