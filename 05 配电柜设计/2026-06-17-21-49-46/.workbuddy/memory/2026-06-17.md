# 2026-06-17 工作记录

## 低压配电系统回路参数计算工具

### v1 — 初版
- tkinter 深色侧边栏 GUI（左侧输入面板 + 右侧回路参数表）
- 计算引擎：三相电流、断路器选型、电缆选型（载流量 + 压降双约束）、保护管径估算
- 支持 Excel 导出（openpyxl）
- **Bug**: _add_section 字段解包错误导致启动崩溃，已修复

### v2 — 按 Excel 模板重建
- 读取用户 Excel 模板（配电系统设计+电缆配置表0616.xlsm）的全部 sheet 数据
- 严格使用模板中的 电缆载流量表、低压电器选型库 数据
- 输出列匹配模板但仍有偏差

### v4 — 三区域布局（当前版本，完全推翻重建）
用户要求：**页面太小 → 全屏三区域；运行方式只有工频/变频；汇总表列严格匹配截图**
- **区域1 左上**: 输入面板 — 回路编号、用途、功率Pe、Kx/cosφ/η/电压、运行方式(工频/变频下拉)、配电形式(MCC/PC)、电缆芯数、长度、压降限值 → 点击"计算并添加"
- **区域2 中间**: 汇总表格（**转置布局**：16行参数 × N列回路）完全匹配截图：
  - 开关柜尺寸 / 单元空间 / 回路用途 / Pe(kW) / Ic(A) / 断路器壳架(A) / In(A) / 配电形式 / 运行方式 / 接触器 / 热继电器 / 变频器 / CT变比 / 电力监控信号 / 线缆型号规格 / 线缆编号
- **区域3 右侧**: 库预览（Tab页切换 电缆载流量表 + 低压电器选型库）
- 电缆选型算法严格对应 VBA SelectCable_Model + CableVDrop_FromModel
- 启动预填8条示例数据
- **BugFix**: 三个核心组件(InputPanel/TransposeTable/LibraryPanel)创建后未pack/grid，导致界面完全空白，已补充`.pack()`调用并修复Excel导出行偏移bug

### v5 — 用户反馈全面修复版（当前版本）
- **数据库弹窗化**: 点击顶部「📚 数据库」按钮弹出独立窗口，不再占右侧空间
  - 电缆载流量库 + 低压电器选型库双Tab
  - 默认锁定 🔒，点锁定按钮切换为解锁 🔓
  - 解锁后：双击编辑单元格、+新增行、−删除行、💾保存
- **默认空白**: 启动不加载任何demo数据
- **回路点击回填编辑**: 点击汇总表中任意回路 → 左侧输入面板回填该回路参数 → 修改后点"保存修改"
- **回路锁定/解锁**: 每列底部有🔓/🔒按钮，锁定后不可删除、不可编辑
  - 顶部「全部加锁」「全部解锁」批量操作
- **回路删除**: 每列底部✕按钮删除
- **输入框深色**: 所有输入框改为深色背景(#21262D)+浅灰文字，不再刺眼白色
- 双区域布局：左侧输入面板(310px) + 右侧汇总表格(转置16行×N列)

### v6 — 柜号管理+上下布局+备用回路（当前版本）
- **上下布局**：输入面板在顶部(水平排列) + 汇总表格在底部(满宽)
- **开关柜代号**：输入基础名称(如"MCC")→ 自动编号 AN01/AN02…
- **单元空间管理**：72E 上限，超限自动新建柜子，碎片空间(<8E)提醒补备用回路
- **计算简化**：Ic = Pe × 2（移除 Kx/cosφ/η/电压设备参数）
- **备用回路**：用途含"备用"→ 弹窗手动选择断路器壳架+脱扣器 → 无接触器/热继/变频器/CT/监控
- **柜体合并显示**：同柜的"开关柜代号""开关柜尺寸"两行合并为一个跨列单元格
- **单元格可复制**：所有数据格改为只读 Entry，点击全选文本(Ctrl+C 复制)
- **署名**：标题栏"中国市政中南院李浩" + 底部状态栏飘蓝署名

### v7 — MCC电器逻辑修复+2行精美输入+表格内联编辑+自动补备用+Excel宋体格式（当前版本）
- **MCC电器逻辑修复**：变频→接触器="/"+热继电器="/"（VFD替代）；工频→变频器="/"（无VFD）
- **输入面板重做为2行精美布局**：Row1=柜代号/回路编号/回路用途/功率/运行方式/配电形式；Row2=柜型/电缆芯数/长度/压降+按钮；暗色卡片式风格+小标题标签
- **表格内联编辑**：点击"配电形式"/"运行方式"/"回路用途"单元格→弹出下拉框或输入框直接修改→自动重算该回路
- **自动补充备用回路**：点击工具栏「🔌 补备用」→扫描所有柜子剩余空间→≥8E补8E备用(脱扣器默认100A可下拉选)、≥4E补8E/2备用(默认63A可下拉选)→自动批量添加，备用回路仅含断路器型号其余为"/"
- **备用回路编辑**：每列底部⚡按钮可修改备用回路断路器参数
- **Excel导出格式修正**：字体宋体10号、列宽20、单行行高18/多行行高20、备用回路浅紫底色
- 新增类：InlineEditPopup（内联编辑弹窗）、AutoSpareDialog（自动补备用配置弹窗）

### v8 — 布局修复+字体增大+必填高亮+默认值修正（当前版本）
- **修复计算按钮不可见**：输入面板高度 160→220px，确保按钮区完整显示
- **增大字体**：输入框字体 10→11号，标签字体 7→9号，表格行高 44/48→56/58px
- **必填项高亮**：开关柜代号/回路编号/回路用途/功率Pe 四个必填输入框使用深蓝底色(input_hl)区分，标签加"＊"标记
- **修复Combobox字体加粗**：运行方式/配电形式下拉框字体与其他输入一致（11号不加粗）
- **默认值修正**：柜子代号默认 "X-"（原 "MCC"）
- **表格列宽增大**：158→180px，行标签宽 158→170px，下面不再空洞
- **Excel导出**：修正 openpyxl 拼写错误（PatternFill/column_dimensions）

### v9 — 1行输入面板+表格内联编辑+监控信号2行+单击回填（当前版本）
- **输入面板改为1行**：10个字段+按钮紧凑排列在一行（柜代号/回路编号/回路用途/功率/运行方式/配电形式/柜型/芯数/长度/压降+按钮），高度130px
- **表格内联编辑（无弹窗）**：
  - 配电形式/运行方式 → 单元格内Combobox下拉框，选择立即重算
  - 回路用途/功率 → 可编辑Entry，回车或失焦提交+重算
  - 不再弹出InlineEditPopup弹窗
- **电力监控信号2行显示**：Text控件（只读可复制）分两行："三相电流,有功电度" / "合/分,故障"
- **单击回填**：单击表格中任意回路行→输入面板自动回填全部参数（取代之前的双击）
- **所有数据可复制**：全部单元格使用Entry/Text只读控件，Ctrl+C直接复制
- **Excel导出**：监控信号分行显示（\n换行），宋体10号/列宽20/行高18-20
- SUMMARY_ROWS改为(name, editable)元组格式，MONITOR_ROW=14特殊处理

### v10 — 拼写错误全面修复+Excel导出重写（当前版本）
- **修复78个拼写错误**：circuits→circuits、Toplevel→Toplevel、Scrollbar→Scrollbar、grid_propagate→grid_propagate、pack_propagate→pack_propagate、highlightthickness→highlightthickness、PatternFill→PatternFill、column_dimensions→column_dimensions、row_dimensions→row_dimensions
- **重写Excel导出函数**：表头写入逻辑完全错误（把参数名当表头写了）→ 修正为：A1="参数"、B1/C1/D1...=各回路编号、A列=参数名、数据按列写入、合并开关柜代号/尺寸单元格
- **标签字体修复**：输入面板标签字体 7→9号，与输入数据字高接近
- **下拉框样式**：InputPanel 中用 readonly Entry 模拟下拉框（需点击触发下拉菜单，待实现）
- **已知待修复**：下拉框样式仍需优化（Combobox与Entry视觉差异）、长度应整数、柜号累加逻辑验证、GUI底部空白调整

### v11 — 自定义下拉框+Excel颜色修复+默认柜代号X（当前版本）
- **自定义下拉框 ComboLikeEntry**：创建 `ComboLikeEntry` 类替代 `ttk.Combobox`，看起来像普通 Entry（左边输入框+右边小箭头按钮），点击箭头弹出选项列表
- **替换所有 Combobox**：InputPanel/SummaryTable内联编辑/SpareBreakerDialog/AutoSpareDialog 中的 `ttk.Combobox` 全部替换为 `ComboLikeEntry`
- **Excel 颜色填充修复**：颜色值添加 `FF` 前缀（ARGB格式），如 `"1A2332"` → `"FF1A2332"`
- **Excel 列宽拼写修复**：`column_dimensions` → `column_dimensions`（正确拼写），`row_dimensions` → `row_dimensions`
- **默认柜代号 = X**：修复 `"X-"` → `"X"`（InputPanel默认值/读取/初始化/清空）
- **长度字段整数**：`cable_len` 从 `float` 改为 `int`
- **表格文字居中**：所有单元格（除第一列参数名）设置 `justify="center"`
- 已知待验证：ComboLikeEntry 下拉窗口位置/样式、表格居中效果、GUI 底部空白

### v12 — SimpleCombo白底黑字下拉框+Excel纯白导出+严重Bug修复（当前版本）
- **SimpleCombo 重写**：替换 ComboLikeEntry，下拉弹出白色背景 Listbox（bg="white" fg="black"），确保任何系统上选项都清晰可见。Entry部分保持深色与普通输入框一致，小▾箭头不突兀
- **严重 Bug 修复 — SpareBreakerDialog/AutoSpareDialog 变量不同步**：创建了独立 StringVar 但 SimpleCombo 内部另有 `_var`，导致用户选择新值后仍读取旧值 → 改用 `cb._var.get()` 替代独立 StringVar
- **严重 Bug 修复 — InputPanel sv 变量覆盖**：`self.fields[key] = sv` 在 combo 分支后执行覆盖 combo 值 → 添加 `continue` 修复
- **Excel 导出完全重写**：移除所有 PatternFill/颜色填充，纯白色背景+黑色文字+黑色边框+宋体10号+列宽20+行高18/20
- **SimpleCombo 8处引用全局替换**：InputPanel/SummaryTable内联编辑/SpareBreakerDialog/AutoSpareDialog

### 技术栈
- Python 3.13 + tkinter（深色主题 Canvas 滚动表格）
- PyInstaller 单文件 EXE（29MB）
- 文件：`lowvolt_calc.py`、`dist/低压配电计算工具.exe`
