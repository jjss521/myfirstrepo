# 2026-06-18 工作日志

## Bug修复: SubsystemEditDialog 缺少 _safe_destroy 方法
- **文件**: `load_calc/pages/equipment_dialogs.py`
- **问题**: `SubsystemEditDialog` 类（第592行）的 `_on_confirm` 和 `_on_cancel` 调用了 `self._safe_destroy()`，但该类未定义此方法，导致点击"新增配电系统"对话框的确定/取消按钮时报错 `AttributeError`
- **修复**: 在 `_validate` 和 `_on_confirm` 之间添加了 `_safe_destroy` 方法（安全释放 grab 资源后销毁对话框），与 `EquipmentGroupEditDialog` 和 `KpKqEditDialog` 保持一致

## Bug修复: KpKqEditDialog 对话框高度不足导致按钮不可见
- **文件**: `load_calc/pages/equipment_dialogs.py`
- **问题**: `KpKqEditDialog`（"编辑同时系数"对话框）geometry 为 `360x220`，内容实际渲染高度约209~230px（标题+2个输入框+提示文字+2个按钮），在不同DPI/字体渲染下极易溢出，导致"确定""取消"按钮被挤出可视区域
- **修复**: 高度从220扩大到320（`360x320`），同步更新居中定位的 y 坐标计算

## 仪表盘改为显示补偿后参数
- **文件**: `load_calc/pages/page_dashboard.py`
- 4个指标卡片标签更新：总能→补偿后有功/无功/视在功率、补偿后功率因数
- refresh() 改用 `subsystem.compensated_pc` / `compensated_qc` 替代 `total_pc` / `total_qc`
- 概述文字 "380V侧（补偿前）" → "（补偿后）"
- 负荷分布表 cosφ 列从 `power_factor_before` → `power_factor_after`
- 修复了因编辑引入的10kV侧段落重复问题

## PyInstaller 打包 (重新打包)
- 修复: build.spec 中 `PIL` 被错误排除，导致运行时 `No module named 'PIL'` 报错（ttkbootstrap 依赖 Pillow）
- 从 excludes 列表中移除 `'PIL'`，重新打包成功
- 输出: `dist/负荷计算系统v4.exe` (36.4MB)

## 代码审查: 配电柜设计项目
- 审查了 `D:\qoderwork\05 配电柜设计\2026-06-17-21-49-46\lowvolt_calc.py` (1562行, v13)
- 产出 `CODE_REVIEW_REPORT.md`：发现 3 项🔴、8 项🟡、6 项💭
- 最严重: 全局可变状态被GUI直接写入、多处 `except: pass` 静默吞错、输入无校验
- 建议单文件拆分为 7 模块分层架构

## 配电柜工具全部问题修复
- **文件**: `lowvolt_calc.py` (1562→1679行)
- 🔴 修复所有 `except: pass` → 改为精确异常捕获 + 错误日志/弹窗
- 🔴 修复全局数据被GUI直写 → 增加格式校验 + "确认覆盖"二次确认
- 🔴 增加输入校验: pe_kw>0, cable_len>0, vdrop_limit 0~20%
- 🟡 提取重复的 `_edit_tree_cell` 公共方法，消除 `_edit_cable_cell`/`_edit_device_cell` 重复
- 🟡 魔法数字常量化: ROW_HEIGHT_NORMAL/MONITOR/OP, COL_WIDTH, PANEL_HEIGHT 等
- 💭 补充 `calc_ic`/`select_shell`/`compute_row`/`unit_to_e` 类型注解
- 💭 删除 `d:fix_typos.py`（非法文件名）
- 💭 更新 `requirements.txt` 和 `.spec` 文件（添加 hiddenimports 和 excludes）
- ✨ 新增 `LogPanel` 底部调试控制台：彩色日志+时间戳+行数限制+清空按钮
- ✨ `_on_table_change` 从空操作改为控制台输出回路统计
- ✨ 关键操作（添加/编辑/重算/清空/导出）均写入控制台日志
